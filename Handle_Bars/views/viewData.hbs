<!doctype html>
<html>
    <head>
        <title>View Data</title>
        
    </head>

    <body>
        <table border='1'>
            <tr>
                <th>Name</th>
                <th>Age</th>
                <th>School</th>
                 {{#if data.if}}
                    <th>A hidden Col</th>
                 {{else}}
                    <th>A normal Col</th>
                 {{/if}}
            </tr>
            <tr>
                <!--> You use data.attributeName 
                      To reach the attribute you want to reach
                      Wrapped it with double curly braces
                <!-->
                <td>{{data.name}}</td>
                <td>{{data.age}}</td>
                <td>{{data.school}}</td>
                {{#if data.if}}
                    <td>hidden file</td>
                    <!-->else 's tag needs to match if's tag<!-->
                {{else}}
                    <td>normal file</td>
                    <tr><td>Normal row and col</td></tr>
                <!-->You have to end the if otherwise HTML won't be parsed<!-->
                {{/if}}
            </tr>
        </table>
         {{!-- used when there is no data needed to be passed to partial --}}
         {{!-- {{> welcome}} --}}

        {{>welcome data}}
    </body>
</html>
